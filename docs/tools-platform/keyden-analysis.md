# Keyden TOTP 验证器分析

## 项目概述

Keyden 是一款简洁优雅的 macOS 菜单栏双因素认证（2FA/TOTP）管理工具，帮助用户安全存储验证码、实现一键复制，并支持多种方式添加账号。

| 项目信息 | 详情 |
|---------|------|
| **项目名称** | Keyden |
| **作者** | tasselx |
| **开源协议** | MIT License |
| **GitHub Stars** | 208 |
| **Forks** | 7 |
| **Commits** | 62 |
| **主要技术** | Swift 5.9 / SwiftUI + AppKit |
| **平台要求** | macOS 12.0+ |
| **发布时间** | 最新版本 v1.0.8（2026年1月） |

## 核心功能特性

### 1. 安全管理

| 功能 | 说明 |
|------|------|
| **安全存储** | TOTP 密钥存储于 macOS 钥匙串，使用系统加密保护 |
| **离线优先** | 无需网络，所有数据本地加密存储 |
| **导入导出** | 支持备份和恢复账号数据 |
| **GitHub Gist 同步** | 可选私有 Gist 同步功能（端到端加密） |

**安全机制详解：**

macOS 钥匙串是 Apple 官方提供的安全存储解决方案，具有以下优势：

- **系统级加密**：钥匙串数据使用用户登录密码加密
- **沙盒隔离**：App Store 版本运行在沙盒中，限制数据访问
- **生物识别集成**：支持 Touch ID 解锁钥匙串
- **访问控制**：可精细控制每个应用的钥匙串访问权限

### 2. 账号管理

| 功能 | 说明 |
|------|------|
| **扫码添加** | 使用 Vision Framework 扫描二维码添加账号 |
| **手动输入** | 通过剪贴板或输入框批量导入 |
| **Google 迁移** | 从 Google Authenticator 导入账号（扫码或链接） |
| **分组显示** | 按发行方（Issuer）分组显示账号 |
| **置顶排序** | 常用账号可置顶，支持拖拽排序 |
| **搜索功能** | 快速搜索账号 |

**支持的账号格式：**

```otpauth://totp/Issuer:account@example.com?secret=JBSWY3DPEHPK3PXP&issuer=Issuer
```

**分组示例：**

```
🐙 GitHub
  ├── user@github.com
  └── work@company.com

☁️ AWS
  ├── root@example.com
  └── dev@example.com

💰 Coinbase
  └── user@example.com
```

### 3. 使用便捷性

| 功能 | 说明 |
|------|------|
| **一键复制** | 点击验证码即可复制到剪贴板 |
| **全局快捷键** | 可自定义快捷键（默认 ⌘⇧K） |
| **CLI 命令行** | 支持命令行调用，用于脚本自动化 |
| **启动登录** | 开机自启动，随时可用 |
| **自动复制** | 复制后自动清除剪贴板（可选） |

**复制行为：**

- 单击：复制验证码
- 右键：更多选项（置顶、删除、导出二维码）
- 复制后剪贴板保留 45 秒（可配置）

### 4. 主题与本地化

| 功能 | 说明 |
|------|------|
| **浅色/深色模式** | 自动跟随系统设置 |
| **多语言支持** | 英语、简体中文、繁体中文、日语 |
| **自适应界面** | 根据系统主题切换界面风格 |

**支持语言：**

- English（英语）
- 简体中文
- 繁體中文（繁体中文）
- 日本語（日语）

### 5. 命令行工具（CLI）

Keyden 内置 CLI 工具，支持脚本集成和自动化。

**安装方式：**

| 方式 | 说明 |
|------|------|
| **一键安装** | 右键账号 → "Copy CLI Command" → 提示安装 |
| **设置安装** | 设置 → General → CLI Tool → Install |
| **手动安装** | 从 `Keyden.app/Contents/Resources/CLI/keyden` 复制 |

**命令用法：**

```bash
# 获取单个验证码
keyden get GitHub

# 指定账号（发行方:账户格式）
keyden get GitHub:user@example.com

# 空格分隔格式
keyden get GitHub user@example.com

# 列出所有账号
keyden list

# 搜索账号
keyden search google

# 显示帮助
keyden help
```

**脚本集成示例：**

```bash
# 获取验证码并复制到剪贴板
keyden get GitHub | pbcopy

# 在脚本中使用
CODE=$(keyden get GitHub)
echo "您的验证码是: $CODE"

# SSH 连接示例
ssh -o "PreferredAuthentications=publickey" user@host \
  -c "echo $(keyden get GitHub)" | pbcopy
```

**多账号处理：**

当同一发行方下有多个账号时，使用 `issuer:account` 格式指定：

```bash
# 多个 GitHub 账号
keyden get GitHub:personal@gmail.com
keyden get GitHub:work@company.com
```

### 6. 同步功能

**GitHub Gist 同步：**

| 特性 | 说明 |
|------|------|
| **加密存储** | 同步数据使用 TOTP 密钥加密 |
| **私有 Gist** | 自动创建私有 Gist（仅自己可见） |
| **增量同步** | 只同步变更的部分 |
| **离线支持** | 无网络时本地使用，有网络时自动同步 |

**设置步骤：**

1. 创建 GitHub Personal Access Token（需要 `gist` 权限）
2. 打开 Keyden 设置 → Sync
3. 输入 Token 并启用同步
4. 数据自动同步到私有 Gist

**Token 创建地址：**
[https://github.com/settings/tokens](https://github.com/settings/tokens)

### 7. 导入功能

**多种导入方式：**

| 方式 | 说明 | 支持格式 |
|------|------|---------|
| **扫码导入** | 扫描二维码（使用摄像头） | otpauth:// URI |
| **链接导入** | 从 Google Authenticator 迁移链接 | otpauth:// URI |
| **批量导入** | 粘贴多个 URI（每行一个） | otpauth:// URI |
| **手动输入** | 输入发行方、账户、密钥 | Base32 格式 |

**Google Authenticator 迁移：**

1. 在 Google Authenticator 中导出迁移二维码
2. Keyden 扫码或使用迁移链接
3. 完成迁移

**迁移链接格式：**

```
https://www.google.com/android/uncrypt.html?data=...
```

## 技术架构分析

### 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **UI 框架** | SwiftUI + AppKit | 混合使用，菜单栏用 AppKit，界面用 SwiftUI |
| **加密库** | CryptoKit | Apple 官方加密框架，支持 HMAC-SHA1 |
| **安全存储** | Keychain Services | macOS 钥匙串服务 |
| **二维码** | Vision Framework | Apple 官方视觉框架 |
| **构建工具** | Makefile | 自动化构建脚本 |
| **包管理** | Makefile | 简化安装命令 |

### 项目结构

```
Keyden/
├── Keyden/                    # 主应用
│   ├── App/                   # 应用入口
│   ├── Views/                 # SwiftUI 视图
│   ├── Models/                # 数据模型
│   ├── Services/              # 服务层
│   │   ├── TOTPService.swift  # TOTP 生成服务
│   │   ├── KeychainService.swift # 钥匙串服务
│   │   └── QRCodeService.swift   # 二维码扫描服务
│   └── Utils/                 # 工具类
├── KeydenCLI/                 # 命令行工具
├── assets/                    # 资源文件
├── docs/                      # 文档
├── Keyden.xcodeproj/          # Xcode 项目文件
├── Makefile                   # 构建脚本
└── README.md                  # 项目说明
```

### TOTP 算法实现

**时间一次性密码（TOTP）原理：**

```
TOTP = HMAC-SHA1(secret, counter)
```

**生成步骤：**

1. 获取当前时间戳（Unix 时间，30 秒为一个时间步长）
2. 将时间戳转换为 8 字节大端序整数
3. 使用 Base32 解码后的密钥和计数器计算 HMAC-SHA1
4. 取 HMAC 结果的最后 4 位作为偏移量
5. 从偏移位置取 4 字节，屏蔽最高位得到 31 位整数
6. 取模 1,000,000 得到 6 位验证码

**算法特点：**

- **时间同步**：服务器和客户端时间必须同步（30 秒窗口期）
- **密钥安全**：密钥仅存储在本地钥匙串
- **一次性**：每个验证码只能使用一次

### 构建配置

**构建命令：**

```bash
# 通用构建（Apple Silicon + Intel）
make build

# 创建 DMG 安装包
make dmg

# 清理构建产物
make clean

# 仅 Apple Silicon
make build-arm

# 仅 Intel
make build-intel

# Universal 构建
make build-all
```

**构建产物：**

- `Keyden.app`：主应用
- `Keyden.dmg`：DMG 安装包
- `KeydenCLI`：命令行工具

### 测试与质量

**当前状态：**

- 无持续集成状态显示
- 无测试代码提及
- 开源维护模式

**可改进方向：**

- 添加 CI/CD 自动化测试
- 添加 TOTP 算法单元测试
- 添加 UI 测试

## 使用场景分析

### 适用人群

| 用户类型 | 使用价值 |
|---------|---------|
| **开发者** | 快速访问 GitHub、GitLab、Docker Hub 等 2FA 码 |
| **系统管理员** | 管理多个云服务账号（AWS、Cloudflare 等） |
| **设计师** | 访问 Figma、Notion 等设计工具 |
| **普通用户** | 管理 Google、Facebook、Twitter 等社交账号 |
| **财务人员** | 管理加密货币交易所、支付平台账号 |

### 典型场景

**场景 1：日常开发工作流**

```
早晨工作开始：
1. Keyden 随系统启动，自动运行在菜单栏
2. 打开 GitHub，提示 2FA 验证
3. 点击 Keyden 中的 GitHub，验证码自动复制
4. ⌘V 粘贴，完成验证
5. 继续工作，无需切换应用
```

**场景 2：多账号管理**

```
管理多个 GitHub 账号：
1. 个人账号：keyden get GitHub:personal@gmail.com
2. 工作账号：keyden get GitHub:work@company.com
3. 客户端账号：keyden get GitHub:client@agency.com

脚本自动化：
#!/bin/bash
ACCOUNT=${1:-GitHub:default}
keyden get "$ACCOUNT" | pbcopy
echo "已复制 $ACCOUNT 的验证码到剪贴板"
```

**场景 3：团队协作**

```
团队共享服务：
1. 使用 Gist 同步功能
2. 团队成员各自添加账号
3. 密钥通过 Gist 加密同步
4. 离职成员自动失去访问权限

CI/CD 集成：
#!/bin/bash
# 发布脚本中使用
TOTP_CODE=$(keyden get DockerHub)
curl -u "username:$TOTP_CODE" https://hub.docker.com/v2/repos/...
```

**场景 4：账号迁移**

```
从 Google Authenticator 迁移：
1. 在旧手机打开 Google Authenticator
2. 导出所有账号为迁移二维码
3. Keyden 扫描二维码
4. 完成迁移，保持原有时效码连续性
```

### 平台 2FA 设置入口

Keyden README 提供了主流平台的 2FA 设置链接：

| 类别 | 平台 |
|------|------|
| **社交通讯** | Google、Facebook、X(Twitter)、Instagram、Discord、Reddit、Slack |
| **开发工具** | GitHub、GitLab、Bitbucket、Docker Hub、npm |
| **云服务** | AWS、Azure、Google Cloud、DigitalOcean、Cloudflare |
| **游戏平台** | Steam、Epic Games |
| **金融服务** | PayPal、Coinbase、Binance |
| **密码管理** | 1Password、Bitwarden |
| **其他服务** | Microsoft、Apple、Amazon、Dropbox、LinkedIn、ProtonMail、Spotify、Shopify、Notion、Figma |

## 安装与配置

### 安装方式

| 方式 | 命令/步骤 | 优点 |
|------|----------|------|
| **Homebrew** | `brew install --cask tasselx/tap/keyden` | 自动管理更新 |
| **手动下载** | 从 Releases 下载 DMG | 无需 Homebrew |

### 安装后配置

**首次启动设置：**

1. **屏幕录制权限**：需要权限以扫描二维码
2. **钥匙串访问**：首次访问钥匙串需授权
3. **通知权限**：可选，验证码复制通知
4. **启动登录**：可选，随系统启动

**权限问题解决：**

| 问题 | 解决方案 |
|------|----------|
| **屏幕录制权限不识别** | 设置 → 隐私与安全 → 屏幕录制 → 删除后重新添加 Keyden |
| **钥匙串授权提示** | 点击 "Always Allow" 或 "Allow" |
| **"Keyden 已损坏"** | 终端执行 `xattr -cr /Applications/Keyden.app` |

### 推荐配置

**开发者配置：**

```
快捷键：⌘⇧K（默认）
启动登录：是
剪贴板清除：30 秒后
分组：按 Issuer 自动分组
搜索：全局搜索
```

**安全敏感用户配置：**

```
快捷键：自定义（如 ⌃⌥K）
启动登录：否（使用时手动打开）
剪贴板清除：立即
Gist 同步：启用加密同步
启动密码：使用系统密码保护
```

## 与类似工具对比

### TOTP 验证器生态

| 工具 | 类型 | 平台 | 价格 | 开源 | 特点 |
|------|------|------|------|------|------|
| **Keyden** | 菜单栏 | macOS | 免费 | ✅ | CLI 强大，Gist 同步 |
| **1Password** | 密码管理 | 全平台 | 订阅 | ❌ | 密码+2FA 一体 |
| **Authy** | 专用验证器 | 全平台 | 免费/付费 | ❌ | 多设备同步 |
| **Google Authenticator** | 专用验证器 | iOS/Android | 免费 | ❌ | 简单可靠 |
| **Microsoft Authenticator** | 专用验证器 | 全平台 | 免费 | ❌ | 微软生态集成 |
| **Bitwarden Authenticator** | 密码管理插件 | 全平台 | 免费 | ✅ | 开源免费 |
| **OTPClient** | 桌面应用 | Linux | 免费 | ✅ | 完全开源 |

### 功能对比表

| 功能 | Keyden | 1Password | Authy | Google Auth |
|------|--------|-----------|-------|-------------|
| **macOS 原生** | ✅ | ✅ | ❌ | ❌ |
| **菜单栏访问** | ✅ | ✅ | ❌ | ❌ |
| **CLI 工具** | ✅ | ❌ | ❌ | ❌ |
| **二维码扫描** | ✅ | ✅ | ✅ | ✅ |
| **批量导入** | ✅ | ✅ | ❌ | ❌ |
| **Gist 同步** | ✅ | ❌ | ❌ | ❌ |
| **分组管理** | ✅ | ✅ | ❌ | ❌ |
| **全局快捷键** | ✅ | ✅ | ❌ | ❌ |
| **多语言** | ✅ | ✅ | ✅ | ✅ |
| **暗色模式** | ✅ | ✅ | ✅ | ✅ |
| **免费使用** | ✅ | ❌ | 部分 | ✅ |

### 选择建议

**选择 Keyden 如果：**
- ✅ 主要使用 macOS
- ✅ 需要 CLI 工具进行自动化
- ✅ 重视开源透明
- ✅ 需要 Gist 同步功能
- ✅ 喜欢简洁的菜单栏应用

**选择 1Password 如果：**
- ✅ 需要密码管理+2FA 一体化
- ✅ 愿意付费订阅
- ✅ 需要跨平台支持
- ✅ 需要高级安全功能（Watchtower）

**选择 Authy 如果：**
- ✅ 需要多设备同步
- ✅ 使用多平台
- ✅ 需要离线备份

**选择 Google Authenticator 如果：**
- ✅ 仅使用移动设备
- ✅ 只需要基础功能
- ✅ 重视简单可靠

## 安全分析

### 安全设计

**密钥存储：**

| 安全措施 | 实现方式 |
|---------|---------|
| **加密存储** | macOS 钥匙串（系统级加密） |
| **隔离存储** | 每个账号密钥独立存储 |
| **内存保护** | 验证码使用后立即清除内存 |
| **剪贴板保护** | 可配置剪贴板自动清除 |

**同步安全：**

| 安全措施 | 实现方式 |
|---------|---------|
| **加密传输** | HTTPS 传输到 GitHub Gist |
| **端到端加密** | 同步数据使用密钥加密 |
| **私有 Gist** | 仅自己可见的私有仓库 |
| **Token 权限** | 仅 `gist` 权限，最小权限原则 |

### 潜在风险

| 风险 | 说明 | 缓解措施 |
|------|------|---------|
| **设备丢失** | macOS 丢失可能导致账号泄露 | 启用 FileVault 磁盘加密 |
| **恶意软件** | 恶意软件可能读取钥匙串 | 保持系统更新，限制应用权限 |
| **同步泄露** | GitHub Token 泄露风险 | 使用最小权限 Token，定期轮换 |
| **剪贴板泄露** | 验证码可能被他应用读取 | 启用自动清除功能 |

### 安全建议

**个人用户：**

1. 启用 macOS 屏幕锁定密码
2. 启用 FileVault 磁盘加密
3. 定期备份账号数据（导出功能）
4. 剪贴板清除时间设置为 30 秒内
5. 不在公共场合使用

**团队用户：**

1. 使用 Gist 同步时启用加密
2. Token 仅赋予必要的最小权限
3. 定期轮换 GitHub Token
4. 离职成员立即撤销同步权限
5. 建立账号备份恢复流程

## 开发与扩展

### 二次开发

**添加新功能：**

1. **添加新同步服务**：
   ```swift
   // 1. 创建同步服务协议
   protocol SyncService {
       func sync(data: Data) async throws
       func fetch() async throws -> Data
   }
   
   // 2. 实现具体服务
   struct DropboxSync: SyncService {
       // 实现 dropbox API
   }
   
   // 3. 注册到同步管理器
   SyncManager.shared.register(DropboxSync())
   ```

2. **添加新导入方式**：
   ```swift
   enum ImportSource {
       case qrCode
       case manual
       case clipBoard
       case file(url: URL)
   }
   
   // 实现对应解析器
   class ImportParser {
       func parse(from source: ImportSource) throws -> [TOTPAccount]
   }
   ```

3. **自定义主题**：
   ```swift
   extension Color {
       static let keydenPrimary = Color("Primary")
       static let keydenSecondary = Color("Secondary")
       static let keydenAccent = Color("Accent")
   }
   ```

### 构建命令

**Debug 构建：**
```bash
make build CONFIGURATION=Debug
```

**Release 构建：**
```bash
make build CONFIGURATION=Release
```

**创建 DMG：**
```bash
make dmg
```

**代码签名（需要开发者证书）：**
```bash
make sign CERTIFICATE="Developer ID Application: Name"
```

### 贡献指南

**Issue 报告：**

- 描述问题复现步骤
- 提供系统版本和 Keyden 版本
- 附上错误日志

**Pull Request：**

1. Fork 项目
2. 创建功能分支
3. 编写测试
4. 提交代码
5. 确保测试通过
6. 提交 PR

**代码风格：**

- 遵循 Swift 代码规范
- 使用 SwiftLint 检查代码
- 添加必要的注释

## 项目活跃度

| 指标 | 数值 | 说明 |
|------|------|------|
| **GitHub Stars** | 208 | 社区认可度良好 |
| **Forks** | 7 | 二次开发活跃度 |
| **Commits** | 62 | 开发活跃度 |
| **Releases** | 9 | 版本发布频率 |
| **Issues** | 0 | 社区反馈良好 |
| **最后更新** | 2026年1月 | 持续维护中 |

**版本历史：**

| 版本 | 发布日期 | 主要变更 |
|------|---------|---------|
| v1.0.8 | 2026-01-08 | 最新版本 |
| v1.0.7 | 2025-12-xxx | 功能增强 |
| ... | ... | ... |

## 优缺点总结

### 优点

✅ **开源免费**：MIT 协议，完全免费使用  
✅ **macOS 原生**：使用 SwiftUI + AppKit，界面美观流畅  
✅ **安全设计**：密钥存储在 macOS 钥匙串，系统级保护  
✅ **CLI 强大**：支持命令行调用，便于自动化  
✅ **Gist 同步**：可选的加密同步功能  
✅ **多语言**：支持中英文等多语言界面  
✅ **离线优先**：无需网络即可使用  
✅ **轻量级**：菜单栏应用，资源占用小  

### 缺点

❌ **仅 macOS**：不支持 Windows 或 Linux  
❌ **无移动端**：不能在手机上使用  
❌ **签名问题**：未付费签名，导致 Gatekeeper 拦截  
❌ **无测试覆盖**：未提及自动化测试  
❌ **社区较小**：208 stars，社区规模有限  
❌ **无 Web 界面**：不能从浏览器访问  

### 与 Alertivity 对比

| 维度 | Keyden | Alertivity |
|------|--------|------------|
| **功能类型** | TOTP 验证器 | 系统监控工具 |
| **菜单栏** | ✅ | ✅ |
| **开源协议** | MIT | GPL-3.0 |
| **CLI 工具** | ✅ 完整 | ❌ |
| **GitHub Stars** | 208 | 137 |
| **技术栈** | SwiftUI + AppKit | SwiftUI |
| **测试覆盖** | 未提及 | 完整测试覆盖 |
| **同步功能** | Gist 加密同步 | ❌ |

## 使用建议

### 新手快速上手

1. **安装**：使用 Homebrew 安装
   ```bash
   brew install --cask tasselx/tap/keyden
   ```

2. **首次配置**：
   - 允许屏幕录制权限
   - 允许钥匙串访问
   - 设置启动登录

3. **添加账号**：
   - 点击菜单栏 "+" 按钮
   - 选择扫码或手动输入
   - 验证测试码

4. **日常使用**：
   - 点击验证码复制
   - 使用全局快捷键 ⌘⇧K
   - 右键管理账号

### 进阶使用

1. **安装 CLI**：
   ```bash
   keyden install-cli
   ```

2. **配置 Gist 同步**：
   - 创建 GitHub Token（gist 权限）
   - 设置 → Sync → 输入 Token

3. **脚本集成**：
   ```bash
   # 添加到 .bashrc 或 .zshrc
   alias gh2fa='keyden get GitHub | pbcopy'
   
   # SSH 登录时自动使用
   alias sshg='ssh -o "PreferredAuthentications=publickey" user@host && keyden get GitHub | pbcopy'
   ```

4. **批量导入**：
   ```bash
   # 导出所有账号
   keyden export > accounts.txt
   
   # 批量导入到新设备
   keyden import < accounts.txt
   ```

### 最佳实践

- **安全**：启用剪贴板自动清除
- **备份**：定期导出备份
- **同步**：使用 Gist 加密同步
- **组织**：按 Issuer 分组，常用账号置顶
- **快捷键**：使用全局快捷键提升效率

## 相关资源

| 资源 | 链接 |
|------|------|
| **GitHub 仓库** | [https://github.com/tasselx/Keyden](https://github.com/tasselx/Keyden) |
| **官网** | [https://tasselx.github.io/Keyden/](https://tasselx.github.io/Keyden/) |
| **项目官网** | [https://tasselx.github.io/Keyden/](https://tasselx.github.io/Keyden/) |
| **Releases** | [https://github.com/tasselx/Keyden/releases](https://github.com/tasselx/Keyden/releases) |
| **Homebrew Tap** | [https://github.com/tasselx/tap](https://github.com/tasselx/tap) |
| **作者其他项目** | [LanShare-Mac](https://github.com/tasselx/LanShare-Mac) |

## 总结

Keyden 是一款设计精良的 macOS TOTP 验证器，其核心优势包括：

1. **原生体验**：完全使用 SwiftUI + AppKit 开发，界面美观且符合 macOS 设计规范
2. **安全可靠**：密钥存储于 macOS 钥匙串，系统级加密保护
3. **CLI 强大**：内置完整命令行工具，支持脚本自动化
4. **同步灵活**：可选 GitHub Gist 同步，端到端加密
5. **开源透明**：MIT 协议，代码公开可审计
6. **多语言支持**：中英文等多语言界面

对于 macOS 用户来说，Keyden 是一个值得考虑的 TOTP 验证器选择，特别是对于需要 CLI 自动化功能的开发者和系统管理员。
